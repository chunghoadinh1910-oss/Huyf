-- ✅ MẪU CHUẨN CHO NGƯỜI MỚI | TÉO HUB 🍎
-- Giữ nguyên giao diện thật, chỉ thêm hướng dẫn 👈 ngay bên cạnh dòng cần sửa
-- CONFIG
local SCRIPT_LINK = "https://raw.githubusercontent.com/chunghoadinh1910-oss/Huyf/refs/heads/main/mauscriptchoaetik.txt"
local COMMON_KEY_LINK = "https://raw.githubusercontent.com/chunghoadinh1910-oss/Huyf/refs/heads/main/User%20common"
local USER_KEY_LINK = "https://raw.githubusercontent.com/chunghoadinh1910-oss/Huyf/refs/heads/main/User%20key"
local GET_KEY_LINK = "https://notevn.com/raw/4xmvxv6z"

local Player = game.Players.LocalPlayer
local Today = os.date("!%Y-%m-%d")

-- ENCODE / DECODE
local function Decode(str)
    local t = {}
    for i = #str, 1, -1 do
        t[#t+1] = string.char(str:byte(i) - 3)
    end
    return table.concat(t)
end

local function IsExpired(exp)
    return Today > exp
end

-- GUI
local gui = Instance.new("ScreenGui", Player.PlayerGui)
gui.ResetOnSpawn = false

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.fromScale(0.32,0.4)
frame.Position = UDim2.fromScale(0.34,0.3)
frame.BackgroundColor3 = Themes[CurrentTheme].BG
frame.Active = true
frame.Draggable = true
Instance.new("UICorner", frame)

local box = Instance.new("TextBox", frame)
box.Size = UDim2.fromScale(0.8,0.15)
box.Position = UDim2.fromScale(0.1,0.3)
box.PlaceholderText = "Enter key..."
box.BackgroundColor3 = Themes[CurrentTheme].Box
box.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", box)

-- CONFIRM
local btn = Instance.new("TextButton", frame)
btn.Size = UDim2.fromScale(0.5,0.15)
btn.Position = UDim2.fromScale(0.25,0.5)
btn.Text = "CONFIRM"
btn.BackgroundColor3 = Themes[CurrentTheme].Button
btn.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", btn)

-- COPY KEY LINK
local copy = Instance.new("TextButton", frame)
copy.Size = UDim2.fromScale(0.35,0.12)
copy.Position = UDim2.fromScale(0.1,0.7)
copy.Text = "📋 GET KEY"
copy.BackgroundColor3 = Color3.fromRGB(120,120,120)
Instance.new("UICorner", copy)

copy.MouseButton1Click:Connect(function()
    setclipboard(GET_KEY_LINK)
    copy.Text = "✅ COPIED"
    task.wait(1)
    copy.Text = "📋 GET KEY"
end)

-- THEME TOGGLE
local themeBtn = Instance.new("TextButton", frame)
themeBtn.Size = UDim2.fromScale(0.35,0.12)
themeBtn.Position = UDim2.fromScale(0.55,0.7)
themeBtn.Text = "🎨 THEME"
themeBtn.BackgroundColor3 = Color3.fromRGB(180,100,255)
Instance.new("UICorner", themeBtn)

themeBtn.MouseButton1Click:Connect(function()
    CurrentTheme = (CurrentTheme == "Dark" and "Neon" or "Dark")
    frame.BackgroundColor3 = Themes[CurrentTheme].BG
    box.BackgroundColor3 = Themes[CurrentTheme].Box
    btn.BackgroundColor3 = Themes[CurrentTheme].Button
end)

-- CHECK KEY
btn.MouseButton1Click:Connect(function()
    local input = box.Text
    local valid = false

    -- COMMON
    local raw = game:HttpGet(COMMON_KEY_LINK)
    local ekey, exp = raw:match("(.+)|(.+)")
    if Decode(ekey) == input and not IsExpired(exp) then
        valid = true
    end

    -- USER
    local users = game:HttpGet(USER_KEY_LINK)
    for line in users:gmatch("[^\r\n]+") do
        local id, rest = line:match("(%d+)%=(.+)")
        if id == tostring(Player.UserId) then
            local k, e = rest:match("(.+)|(.+)")
            if Decode(k) == input and not IsExpired(e) then
                valid = true
                break
            end
        end
    end

    if valid then
        gui:Destroy()
        loadstring(game:HttpGet(SCRIPT_LINK,true))()
    else
        btn.Text = "❌ INVALID"
        task.wait(1)
        btn.Text = "CONFIRM"
    end
end)

repeat task.wait() until game:IsLoaded()
local savedKey, savedExp = LoadKey()

if savedKey and savedExp and not IsExpired(savedExp) then
    -- kiểm tra lại key với server (github)
    local valid = false

    -- COMMON KEY
    local raw = game:HttpGet(COMMON_KEY_LINK)
    local ekey, exp = raw:match("(.+)|(.+)")
    if Decode(ekey) == savedKey and not IsExpired(exp) then
        valid = true
    end

    -- USER KEY
    local users = game:HttpGet(USER_KEY_LINK)
    for line in users:gmatch("[^\r\n]+") do
        local id, rest = line:match("(%d+)%=(.+)")
        if id == tostring(game.Players.LocalPlayer.UserId) then
            local k, e = rest:match("(.+)|(.+)")
            if Decode(k) == savedKey and not IsExpired(e) then
                valid = true
                break
            end
        end
    end

    if valid then
        loadstring(game:HttpGet(SCRIPT_LINK,true))()
        return
    else
        DeleteKey()
    end
end

-- 🟢 Nút mở menu chính
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "http://www.roblox.com/asset/?id=15059956542"  -- 👈 đổi ID ảnh nút mở menu tại đây (ảnh roblox)

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- 🟣 Giao diện Fluent
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat wait() until game:IsLoaded()

-- 🪟 Cửa sổ chính
local Window = Fluent:CreateWindow({
	Title = "mẹ mày béo =}",         -- 👈 đổi thành tên script của bạn
	SubTitle = "vip pro💎👹",              -- 👈 đổi mô tả phụ
	TabWidth = 157,
	Size = UDim2.fromOffset(450, 300),
	Acrylic = true,
	Theme = "Aqua",                   -- 👈 đổi màu chủ đạo: Amethyst, Dark, Aqua, Light,...
	MinimizeKey = Enum.KeyCode.End
})

-- 📑 Các tab (mục chính)
local Tabs = {
	Main0 = Window:AddTab({ Title = "cdvn" }),  -- 👈 đổi tên tab 1
	Main1 = Window:AddTab({ Title = "fling thing and people" }),  -- 👈 đổi tên tab 2
	Main2 = Window:AddTab({ Title = "bran rót💩" }),     -- 👈 đổi tên tab 3
	Main3 = Window:AddTab({ Title = "99 đêm🥷" }),        -- 👈 đổi tên tab 4
	Main4 = Window:AddTab({ Title = "blox fruit🍎" }),    -- 👈 đổi tên tab 5
	Main5 = Window:AddTab({ Title = "plan vs branro🌲🐘t" }) -- 👈 đổi tên tab 6
}

------------------------------------------------
-- TAB 1: Các kênh tiktok
------------------------------------------------
Tabs.Main0:AddButton({
	Title = "bố mày bị đình chỉ",              -- 👈 tên nút
	Description = "oách xà lách vô cùng",-- 👈 mô tả nút
	Callback = function()
		setclipboard("https://www.tiktok.com/@jannie3342?_t=ZS-90QsJQ5j0NO&_r=1")  -- 👈 link sao chép
	end
})

Tabs.Main0:AddButton({
	Title = "tik",
	Description = "làm chơi thui",
	Callback = function()
		setclipboard("https://www.tiktok.com/@b.yt03?_t=ZS-90QsHokNlwN&_r=1")     -- 👈 link khác
	end
})

Tabs.Main0:AddButton({
	Title = "tiktok",
	Description = "",
	Callback = function()
		setclipboard("https://www.tiktok.com/@b.yt03?_t=ZS-90QsHmSjaiu&_r=1")
	end
})

------------------------------------------------
-- TAB 2: Ví dụ thêm script
------------------------------------------------
Tabs.Main1:AddButton({
	Title = "auto click ( nokey )",                             -- 👈 tên nút hiển thị
	Description = "tăng khả năng ăn đc kick",-- 👈 mô tả
	Callback = function()
		local Settings = {
			JoinTeam = "Pirates";   -- 👈 team mặc định: Pirates hoặc Marines
			Translator = true;      -- 👈 bật dịch sang tiếng Việt nếu có
		}
		loadstring(game:HttpGet('https://raw.githubusercontent.com/catwljz/dz/refs/heads/main/autoclick.txt'))() -- 👈 link script tải
	end
})
Tabs.Main1:AddButton({
	Title = "orange hub",                             -- 👈 tên nút hiển thị
	Description = "khá lag",-- 👈 mô tả
	Callback = function()
		local Settings = {
			JoinTeam = "Pirates";   -- 👈 team mặc định: Pirates hoặc Marines
			Translator = true;      -- 👈 bật dịch sang tiếng Việt nếu có
		}
		loadstring(game:HttpGet("https://raw.githubusercontent.com/HieuDepTrai-Z/ScriptOrange/refs/heads/main/CamHub.lua"))() -- 👈 link script tải
	end
})
Tabs.Main2:AddButton({
	Title = "auto click ( nokey )",                             -- 👈 tên nút hiển thị
	Description = "",-- 👈 mô tả
	Callback = function()
		local Settings = {
			JoinTeam = "Pirates";   -- 👈 team mặc định: Pirates hoặc Marines
			Translator = true;      -- 👈 bật dịch sang tiếng Việt nếu có
		}
		loadstring(game:HttpGet('https://raw.githubusercontent.com/catwljz/dz/refs/heads/main/autoclick.txt'))() -- 👈 link script tải
	end
})
Tabs.Main2:AddButton({
	Title = "tăng lực ném ( nokey )",                             -- 👈 tên nút hiển thị
	Description = "tăng khả năng ăn đc kick",-- 👈 mô tả
	Callback = function()
		local Settings = {
			JoinTeam = "Pirates";   -- 👈 team mặc định: Pirates hoặc Marines
			Translator = true;      -- 👈 bật dịch sang tiếng Việt nếu có
		}
		local Workspace = game:GetService("Workspace") local Debris = game:GetService("Debris") local strength = 850 local fling = true Workspace.ChildAdded:Connect(function(model) if model.Name == "GrabParts" then local partInfo = model:FindFirstChild("GrabPart") if not partInfo then return end local weld = partInfo:FindFirstChild("WeldConstraint") if not weld or not weld.Part1 then return end local part = weld.Part1 local velocity = Instance.new("BodyVelocity") velocity.Parent = part model:GetPropertyChangedSignal("Parent"):Connect(function() if not model.Parent then if fling then velocity.MaxForce = Vector3.new(1e9, 1e9, 1e9) velocity.Velocity = Workspace.CurrentCamera.CFrame.LookVector * strength else velocity.MaxForce = Vector3.zero end Debris:AddItem(velocity, 1) end end) end end)-- 👈 link script tải
	end
})
